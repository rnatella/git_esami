Copiare qui dentro i file di uno qualunque degli esercizi del corso.

Effettuare il commit e il push dei file, usando i comandi:

export GIT_SSL_NO_VERIFY=1
git add --all
git commit -m "soluzione cognome nome"
git push
